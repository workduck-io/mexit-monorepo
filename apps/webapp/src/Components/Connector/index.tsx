import React from 'react'

import useExtensionConnector from './useExtensionConnector'

/* 
 * <Web app - extension> connector
  Renders in `Iframe`
*/
const Connector = () => {
  useExtensionConnector()

  return <>----</>
}

export default Connector
