import styled from 'styled-components'

export const Button = styled.input`
  width: fit-content;
  background: #111827;
  color: #fff;
  border-radius: 0.25rem;
  border: none;
  padding: 0.5rem 1rem;
  line-height: 1.25rem;
  font-size: 0.9rem;

  cursor: pointer;
`
