version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12

    commands:
      - npm install yarn
      - yarn

  build:
    commands:
      - yarn nx release webapp

artifacts:
  files:
    - "**/*"
  base-directory: "dist/apps/webapp"
